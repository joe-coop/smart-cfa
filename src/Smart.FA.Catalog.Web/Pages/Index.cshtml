@page
@model Web.Pages.IndexModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Index";
    ViewData["ToolBarTitle"] = "Add a new training";
}


<smart-card>
    <smart-card-body>
        <smart-card-title text=@("Hi " + (Model.Identity?.Trainer.Name.FirstName ?? "Unknown") + "!")></smart-card-title>
    </smart-card-body>
</smart-card>
